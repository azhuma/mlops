# Исправлены ошибки в билде для занятия 3

# рассмотрим опцию restart: always
# Найдем id контейнера
# заменить на корректный айди контейнера cb:mysql
docker ps
docker exec -it <container id> sh

# внутри контейнера найти pid и прибить
kill <pid>

# заново запустим список контейнеров
docker compose ps

# проверяем время запуска контейнера
# видим что он стартовал недавно
# в целом можно все то же самое сделать одной командой
docker exec <container id> kill 1

# в докере внутри контейнера pid=1 всегда самый главный

# environment variable
# зайдем в наш контейнер снова
docker exec <container id> bash

# посмотрим переменные среды внутри контейнера
env

# также можно
echo $DATABASE_URL
export x=30
echo $x

